Una vez descargado el repositorio se debe ejecutar el siguiente comando:
<br>
Instalar dependencias de composer:
<pre>composer install</pre> 
instalar dependencias de NPM:
<pre>npm install</pre> o <pre>npm i</pre>
